import { myPluginSoftwareCatalogPlugin } from './plugin';

describe('my-plugin-software-catalog', () => {
  it('should export plugin', () => {
    expect(myPluginSoftwareCatalogPlugin).toBeDefined();
  });
});
